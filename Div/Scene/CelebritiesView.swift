import SwiftUI

struct CelebritiesView: View {
    // MARK: - PROPERTIES

    // MARK: - BODY
    var body: some View {
        NavigationView {
            Text("Osobnosti")
                .navigationTitle("Osobnosti")
        }
    }
}

// MARK: - PREVIEW
#Preview {
    CelebritiesView()
}
