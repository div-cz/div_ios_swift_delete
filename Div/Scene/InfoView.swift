import SwiftUI

struct InfoView: View {
    // MARK: - PROPERTIES

    // MARK: - BODY
    var body: some View {
        NavigationView {
            Text("Info & kontakty")
                .navigationTitle("Info")
        }
    }
}

// MARK: - PREVIEW
#Preview {
    InfoView()
}
